#! /usr/bin/python

from cronRunner.cronRunner import cronRunner
# from common.functions import *

# update SO
# cmd('apt update -y > /dev/null 2>&1', 'Updating SO...')

# create cron runner for watcher
cronRunner()
